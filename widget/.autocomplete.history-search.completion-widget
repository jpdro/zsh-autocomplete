#!/bin/zsh

_main_complete
local ret=$?
case $WIDGET in
  *-backward)
    compstate[insert]='menu:0'
    ;;
  *-forward)
    compstate[insert]='menu:1'
    ;;
esac
compstate[list]='list force'
return ret
