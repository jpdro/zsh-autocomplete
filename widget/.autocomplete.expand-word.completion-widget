#!/bin/zsh

local curcontext; .autocomplete.curcontext expand-word

local completer=$_lastcomp[completer]
_main_complete
local ret=$?
[[ $compstate[old_list] == keep ]] || completer=$_lastcomp[completer]
if [[ $completer == extras ]]; then
  compstate[insert]='0'
  compstate[list]=''
fi
return ret
